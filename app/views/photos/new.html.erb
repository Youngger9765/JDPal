photo new!

<%=form_for @photo, :url => user_photos_path(current_user,current_user), :method => :POST do |f| %>

  <div class="form-group">
      <%=f.label :image, "相片"%>
      <%=f.file_field :image%>
  </div>

  <%= f.submit "上傳", class: "btn btn-primary form-control" %>

<% end %>